# 阶段 0 实施前基线

本目录记录 2026-08-17 执行的阶段 0 实施前基线。基线编号为：

```text
stage0-baseline-20260817-001
```

本次基线只记录现状，不修改业务代码、不修改主数据库业务数据。数据库备份和恢复库位于仓库外/被 `.gitignore` 忽略的临时目录，不能提交到 Git。

## 结果概览

| 项目 | 结果 |
|---|---|
| 后端工作区 | 干净 |
| 后端测试 | 通过，56 个测试，0 失败 |
| 后端 JAR 构建 | 通过 |
| 前端依赖重装 | 通过，318 个包 |
| 前端类型检查 | 通过 |
| 前端生产构建 | 通过，714 个模块 |
| MySQL 连接 | 可用，8.0.41 |
| 数据库备份 | 已完成 |
| 备份恢复 | 已恢复到隔离数据库 |
| API 健康检查 | 通过 |
| API 核心冒烟 | 在隔离恢复库通过 |
| Docker 构建 | 未执行，Docker daemon 未启动 |
| 密钥扫描 | 未执行 Gitleaks，工具未安装；已完成静态配置检查 |

## 必须先处理的问题

1. 数据库存在仓库初始化脚本未覆盖的 RBAC 表：`tenant`、`role`、`permission`、`role_permission`、`user_role`。
2. 主库存在 1 条活跃项目缺失用户、2 条活跃任务缺失项目的孤儿数据。
3. JWT 密钥在 Java 源码中硬编码。
4. 开发和测试配置存在固定数据库口令，AI 配置存在默认值/占位配置。
5. 当前没有 GitHub Actions、Gitleaks 和前端自动化测试脚本。
6. Docker Desktop daemon 当前未运行，因此 Docker 镜像构建尚未形成基线。

## 后续顺序

应先完成数据差异审计和凭据治理，再设计 Flyway V1；不能直接把当前 `sql/` 文件拼接成 V1。

