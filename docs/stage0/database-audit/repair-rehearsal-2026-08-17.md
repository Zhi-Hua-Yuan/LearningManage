# 阶段 0.2 孤儿数据修复演练记录

执行日期：2026-08-17 Asia/Shanghai  
演练数据库：`learning_manage_baseline_restore_20260817`  
主数据库：`learning_manage`，只读复核，未执行写操作  
执行状态：通过，恢复库最终保留在“已修复”状态

## 1. 已确认决策

- RBAC 五张表保留为 V1 正式权限模型，阶段 0 暂不接入业务代码或自动迁移用户角色。
- 1 个活跃孤儿项目和 2 个活跃孤儿任务先在恢复库演练逻辑删除。
- 既有的 1 个已逻辑删除孤儿任务保持原值。
- 本次确认不包含主库写入授权。

## 2. 目标和保护条件

脚本不保存原始主键，仅使用 12 位 SHA-256 主键指纹、原始时间和旧状态定位：

| 类型 | 指纹 | 演练前状态 | 预期操作 |
|---|---|---|---|
| 项目 | `10da6d2c6938` | 活跃、用户缺失、无任务和里程碑 | `is_delete=1`，写入 `deleted_at` |
| 任务 | `80d94ad62151` | 活跃、项目缺失 | `is_delete=1`，`delete_source=1` |
| 任务 | `4693a21c3076` | 活跃、项目缺失 | `is_delete=1`，`delete_source=1` |
| 任务 | `110aa8b40378` | 已逻辑删除 | 不修改 |

修复脚本固定使用恢复库，并包含以下失败即停止条件：

- 当前数据库必须是指定恢复库。
- 候选必须恰好为 1 个项目和 2 个任务。
- 更新影响行数必须恰好为 1 和 2。
- 不使用物理 `DELETE`。

## 3. 演练过程

第一次应用修复：

- 修复时间标记：`2026-08-17 12:52:34`。
- 项目影响行数：1。
- 任务影响行数：2。
- 候选和影响行数保护全部通过。

回滚验证：

- 使用相同修复时间作为批次标记。
- 恢复项目：1。
- 恢复任务：2。
- 回滚后重新出现 1 个活跃孤儿项目和 2 个活跃孤儿任务。
- 项目、任务的旧逻辑删除状态、删除来源和更新时间均恢复到审计前值。
- 原本已删除的任务保持不变。

第二次应用修复并形成最终状态：

- 最终修复时间标记：`2026-08-17 12:53:34`。
- 项目影响行数：1。
- 任务影响行数：2。

## 4. 最终验证

| 检查 | 结果 |
|---|---:|
| 活跃孤儿项目 | 0 |
| 活跃孤儿任务 | 0 |
| 活跃孤儿里程碑 | 0 |
| 活跃孤儿团队成员 | 0 |
| 目标项目已逻辑删除 | 1 |
| 两个目标任务已逻辑删除 | 2 |
| 原已删除任务保持不变 | 1 |
| 非法逻辑删除标志 | 0 |
| 非法任务状态 | 0 |
| 非法任务优先级 | 0 |

恢复库物理行数为用户 29、项目 36、任务 123。本次只更新逻辑删除字段，修复前后物理行数不变。恢复库曾用于 API 冒烟测试，因此包含只存在于隔离库的测试记录；主库对应物理行数仍为用户 28、项目 35、任务 122。

后端回归测试：56 个测试全部通过，失败 0、错误 0、跳过 0。

## 5. 主库隔离验证

演练结束后只读复核主库：

- 物理行数仍为用户 28、项目 35、任务 122。
- 仍有 1 个活跃孤儿项目和 2 个活跃孤儿任务。
- 三条目标记录的逻辑删除状态、删除来源和更新时间与演练前一致。

以上结果证明修复只作用于恢复库，主库未被修改。

## 6. 风险与下一门槛

数据关联风险在恢复库中已从高风险降为无活跃孤儿关联，结论置信度高。逻辑删除仍会让目标数据从正常业务查询中消失，因此主库执行前仍须满足：

1. 重新生成并校验主库备份。
2. 重新运行只读候选审计，确保目标指纹和旧值没有变化。
3. 人工复核修复 SQL 和回滚 SQL。
4. 明确批准主库维护窗口和写入操作。
5. 主库执行后立即运行验证 SQL，并保留前后证据。

当前不具备第 4 项授权，因此主库修复未执行。
