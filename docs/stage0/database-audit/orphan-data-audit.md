# 孤儿数据溯源报告

本报告不保存原始用户、项目、任务 ID。受限 ID 指纹和完整查询输出保存在：

```text
D:\ajavacode\LearningManage\.codex-tmp\stage0-audit-20260817
```

## DQ-001：孤儿项目

特征：

- 活跃项目数量：1。
- 所属用户不存在。
- 团队为空。
- 没有关联任务。
- 没有关联里程碑。
- 没有关联 AI 重排操作。
- 创建时间和更新时间均为 2026-04-11。

当前不能从现有业务关系推断正确用户，因此不允许自动转移给任意现有用户。

候选处理顺序：

1. 在历史备份中查找缺失用户。
2. 如果用户可恢复，恢复用户后重新验证项目权限。
3. 如果用户无法恢复，确认项目是否属于测试或无效数据。
4. 经过审批后逻辑删除项目，保留审计记录。

## DQ-002：孤儿任务

特征：

- 指向同一个不存在的项目。
- 活跃任务：2 条。
- 逻辑删除任务：1 条。
- 三条任务的所属用户仍然存在。
- 创建时间集中在 2026-03-24～2026-03-25。
- 没有关联里程碑。
- 没有 AI 重排记录。
- 没有任务状态幂等记录。
- 没有任务标题改名日志。

当前证据表明任务的用户还存在，但原项目已经不在 `project` 表中。由于没有项目快照或业务日志，不能自动创建一个新项目来承接这些任务。

候选处理顺序：

1. 查找外部备份或历史导出中的原项目。
2. 如果能确认原项目，恢复项目并重新核对任务。
3. 如果不能恢复，人工确认任务是否仍有业务价值。
4. 无业务价值时，对任务执行带原因的逻辑删除。
5. 有业务价值但无法恢复父项目时，等待产品规则决定是否允许“待归档项目”承接。

## 当前结论

三条任务和一条项目均不适合自动修复。当前阶段只完成溯源，不执行写操作。

