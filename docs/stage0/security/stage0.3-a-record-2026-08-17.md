# 阶段 0.3-A 执行记录

状态：代码和配置治理已完成，真实凭据尚未轮换。  
范围：环境变量外置、JWT 服务化、生产启动门禁、Compose 门禁、测试和文档。

## 完成内容

- 移除开发、测试和生产配置中的固定数据库密码。
- 移除生产配置中的模型 API Key。
- 移除静态 JWT 密钥，改为 `JwtProperties` 和 `JwtTokenService`。
- 修复无效 JWT 被拦截器接受的问题：解析失败现在会返回未登录错误。
- 生产 Profile 对数据库密码、JWT 密钥和模型 Key 执行启动校验。
- 增加 JWT 密钥长度和占位值检查。
- 增加 `.env.example`、安全启动说明和脱敏盘点记录。
- Docker Compose 改用必填变量保护数据库、JWT 和模型配置。
- 生产配置纳入 Git 管理范围，真实本地配置改为忽略。

## 未执行内容

- 未轮换数据库密码。
- 未轮换 JWT 密钥。
- 未轮换阿里云 API Key。
- 未修改 Redis 真实密码。
- 未接入 Qdrant。
- 未接入 RBAC。

## 验收证据

- 后端测试：61 个测试全部通过，失败 0、错误 0。
- 测试上下文显式使用 `test` Profile，并通过临时 `TEST_DB_PASSWORD` 连接测试库。
- JWT 签发、解析、空 Token、篡改 Token 测试通过。
- 生产密钥策略对缺失、占位值和合法值的单元测试通过。
- 开发 Profile 使用临时环境变量启动成功，`GET /api/health` 返回 HTTP 200，验证后进程已停止。
- 生产 Profile 缺少密钥时启动以非零状态退出，并命中 JWT 配置门禁。
- Docker Compose 使用 `.env.example` 通过 `docker compose config --quiet` 解析；Docker daemon 未启动，因此未执行容器部署。
- 生产启动行为仍需在独立维护环境中使用真实部署变量做一次完整启动验证；本阶段不提供真实凭据，因此不执行真实生产启动。

已有运行实例在应用重启前必须从受控存储注入当前 JWT 密钥，以避免把配置外置误当成凭据轮换；真正的密钥轮换保留到阶段 0.3-B。
