# 阶段 0.3-B2-0 执行前门禁记录

执行日期：2026-08-18 Asia/Shanghai
基线提交：`6e89594 docs(security): 完成阶段0.3-B1凭据轮换准备`
当前状态：B2-0 门禁通过；B2-1 可开始

## 1. 已完成检查

- B1 凭据清单、就绪报告和运行手册均存在。
- B1 提交已存在；本次 B2-0 配置修正和门禁记录产生新的待提交修改。
- 应用端口 `8123` 当前未监听。
- 当前没有发现仓库根目录 `.env`、`.env.local` 或 `.env.*.local` 文件。
- 尚未创建业务数据库账号、修改数据库权限、修改数据库密码或轮换任何外部凭据。
- 已新增隔离测试库 `learning_manage_stage0b2_test_20260818`，仅导入 B1 已验证的 schema-only 备份；未修改现有 `learning_manage` 或 `learning_manage_test`。

## 2. B1 备份证据复核

以下文件存在且 SHA-256 与 B1 就绪报告一致：

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `learning_manage-full.sql` | 133107 | `29A1CA52DB30D1132083D324AF2721217FF9583B43F7479639D59577FFEA3432` |
| `learning_manage-schema.sql` | 27971 | `F19C30AA58E4E34256352A0327DD448BE7C0E460CE2D8B9A824B06ABC2D800CB` |
| `restore-mysqlcheck.txt` | 1327 | `608B01848BD98EE891BB750F469F89E3B0932C5768108DA27698A63302C9D343` |
| `restore-counts.tsv` | 162 | `D278168128B481086CF9B8358638FD6DB4669FB5517739F1F5E54C14B42788CF` |

## 3. 本次配置修正

已将开发和测试配置中的 Root 默认用户名移除：

- `application-dev.yml`：`${DB_USERNAME:root}` → `${DB_USERNAME}`
- `application-test.yml`：`${TEST_DB_USERNAME:root}` → `${TEST_DB_USERNAME}`

缺少业务账号环境变量时，应用现在会停止解析配置，不会回退到 Root。

## 4. 自动化检查

已执行安全相关测试：

- `ProductionSecretPolicyTest`：3 项通过。
- `JwtTokenServiceTest`：2 项通过。
- 合计：5 项通过，失败 0，错误 0，跳过 0。

## 5. 数据库只读门禁

使用受保护的 Windows 用户级管理员变量完成只读连接，未输出密码。

检查结果：

- MySQL：8.0.41，认证用户为 `root@localhost`。
- Root：`mysql_native_password`，未锁定，密码未过期，仍具有全局授权能力。
- `learning_manage_app@localhost`：不存在。
- `learning_manage_test_app@localhost`：不存在。
- `learning_manage`：存在，项目 35、任务 122，活跃孤儿项目 0、活跃孤儿任务 0。
- `learning_manage_test`：存在，但当前只有 10 张表，包含 `user`、`project`、`milestone`、`task`、`weekly_review` 和 RBAC 表。
- `learning_manage_test` 缺少 `team`、`team_member` 以及 AI 相关业务表，不能作为主库结构等价的完整测试库。
- 新隔离测试库 `learning_manage_stage0b2_test_20260818` 已从 schema-only 备份导入，包含 20 张表，数据行数为 0。
- 新隔离测试库的活跃孤儿项目、任务、里程碑、团队成员均为 0；主库对应四类活跃孤儿数据也均为 0。
- `learning_manage_app@localhost` 和 `learning_manage_test_app@localhost` 均不存在。
- `learning_manage`、`learning_manage_test` 和新隔离测试库均可通过管理员只读连接访问。

除新增隔离测试库及导入其 schema-only 结构外，本次查询只包含 `SELECT` 和 `SHOW GRANTS`，没有创建或修改任何 MySQL 账户。仓库中的 `sql/audit/stage0_b2_admin_readonly_gate.sql` 直接检查旧 `learning_manage_test` 时曾在 `team_member` 处因表不存在而停止；现已通过新隔离测试库完成等价门禁，确认原停止是结构差异而非认证失败。

## 6. B2-0 结论

B2-0 主库、隔离测试库和凭据门禁均通过：

- B1 备份和恢复证据有效。
- 开发、测试配置不再默认使用 Root 用户名。
- Root 管理连接可用且状态已确认。
- 主库存在且状态正常。
- 原 `learning_manage_test` 保持不变并记录为旧的精简测试库；新隔离测试库结构与 B1 主库 schema 等价，可用于后续 B2 测试。
- 两个业务账号尚未创建。
- 主库和新隔离测试库当前没有活跃孤儿项目、任务、里程碑或团队成员。
- B2-0 安全测试 5 项全部通过。

下一步可进入 B2-1：创建本机最小权限的 `learning_manage_app@localhost` 和独立测试账号，并将应用连接目标切换到新隔离测试库。后续仍未执行 JWT 轮换、阿里云 Key 轮换或 Root 密码轮换。
