apiVersion: 1
providers:
  - name: LearningManage
    folder: LearningManage
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
