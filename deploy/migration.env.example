# Create /etc/learning-manage/migration.env only for the migration window.
# The production scripts require this exact fixed username and remove the
# production path automatically when migration finishes.
FLYWAY_DB_USERNAME=learning_manage_migrator
FLYWAY_DB_PASSWORD=REPLACE_WITH_32_TO_128_URL_SAFE_CHARACTERS
